`$=dv.list(dv.pages().sort(f=>f.file.mtime.ts,"desc").limit(5).file.link)`


```dataviewjs
await dv.list(
  (await dv.pages())
    .sort(f => f.file.mtime.ts, "desc")
    .limit(5)
    .file.link
)
```